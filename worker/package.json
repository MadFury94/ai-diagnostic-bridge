{
  "name": "ai-diagnostic-bridge-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/node": "latest",
    "playwright-core": "^1.63.0",
    "typescript": "~5.8.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.130.0"
  }
}
